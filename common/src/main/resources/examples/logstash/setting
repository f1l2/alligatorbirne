input {
  file {
    path => "${main.basedir}\\workbench\\logs\\ep.log"
    type => "ep"  
    start_position => "beginning"
  }
  file {
    path => "${main.basedir}\\workbench\\logs\\dev.log"
    type => "ep"  
    start_position => "beginning"
  }
  file {
    path => "${main.basedir}\\workbench\\logs\\cm.log"
    type => "ep"  
    start_position => "beginning"
  }
}